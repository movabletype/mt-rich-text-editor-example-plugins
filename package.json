{
  "name": "mt-rich-text-editor-example-plugins",
  "type": "module",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "rm -rf dist && for d in packages/*; do if [ -d $d ]; then tsx build.ts $d dist; fi; done"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^24.5.2",
    "js-yaml": "^4.1.0",
    "tsx": "^4.20.5"
  }
}
